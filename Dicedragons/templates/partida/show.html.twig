{% extends 'base.html.twig' %}

{% block title %}Partida{% endblock %}

{% block body %}
    <h1>Partida</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ partida.id }}</td>
            </tr>
            <tr>
                <th>Proxima_sesion</th>
                <td>{{ partida.ProximaSesion ? partida.ProximaSesion|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('partida_index') }}">back to list</a>

    <a href="{{ path('partida_edit', {'id': partida.id}) }}">edit</a>

    {{ include('partida/_delete_form.html.twig') }}
{% endblock %}
