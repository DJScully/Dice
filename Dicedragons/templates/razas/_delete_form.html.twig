<form method="post" action="{{ path('razas_delete', {'id': raza.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ raza.id) }}">
    <button class="btn">Delete</button>
</form>
